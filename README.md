# Realtime Chat

A simple real-time chat application built with Node.js, Express, and Socket.IO.

## Run locally

1. Install dependencies:

   ```bash
   npm install
   ```

2. Start the server:

   ```bash
   npm run dev
   ```

3. Open `http://localhost:3000` in two browser tabs/windows to test real-time messaging.
